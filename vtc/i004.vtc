varnishtest "Verify simple requests."

shell {
    echo "[test]\n"\
    "Cache1 = http://localhost:6001\n"\
    "Cache2 = http://localhost:6002\n"\
    "Cache2 = http://localhost:6003" > caches.ini
}

shell -err -expect "Cache2 duplicated" { ${broadcaster} -cfg caches.ini }
